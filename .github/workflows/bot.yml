name: Last.fm Ranking Tracker

on:
  schedule:
    - cron: "3,13,23,33,43,53 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test scheduled run
        run: |
          echo "================================"
          echo "SCHEDULE TEST"
          echo "Time:"
          date
          echo "================================"
